import { SplitFirstCommaPipe } from './split-first-comma.pipe';

describe('SplitFirstCommaPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitFirstCommaPipe();
    expect(pipe).toBeTruthy();
  });
});
