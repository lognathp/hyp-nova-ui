import { DisplayQuantityPipe } from './display-quantity.pipe';

describe('DisplayQuantityPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayQuantityPipe();
    expect(pipe).toBeTruthy();
  });
});
