import { RemoveSpecialCharacterPipe } from './remove-special-character.pipe';

describe('RemoveSpecialCharacterPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveSpecialCharacterPipe();
    expect(pipe).toBeTruthy();
  });
});
